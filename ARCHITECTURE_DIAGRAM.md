# DatabaseAI Architecture Diagram

## System Overview

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              FRONTEND (React)                                │
│  ┌──────────────┐        ┌──────────────┐         ┌──────────────┐         │
│  │ Connection   │───────▶│  Chat Page   │◀───────│  Data Table  │         │
│  │    Page      │        │  (Queries)   │        │   (Results)  │         │
│  └──────────────┘        └──────────────┘         └──────────────┘         │
│         │                        │                                           │
│         │ session_id             │ session_id + query                       │
│         ▼                        ▼                                           │
└─────────┼────────────────────────┼───────────────────────────────────────────┘
          │                        │
          │  HTTP/JSON             │
          │                        │
┌─────────▼────────────────────────▼───────────────────────────────────────────┐
│                           BACKEND (FastAPI)                                  │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │                         API Routes                                      │ │
│  │  /database/connect  →  Create Session + Pool                          │ │
│  │  /query            →  Execute with Session                            │ │
│  │  /stats/pools      →  Monitor Connection Pools                        │ │
│  │  /stats/sessions   →  Monitor Active Sessions                         │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                │                                              │
│                                ▼                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │                      Session Manager                                    │ │
│  │  • Manages user sessions (UUID-based)                                  │ │
│  │  • 60-minute timeout                                                   │ │
│  │  • Schema caching per session                                          │ │
│  │  • Auto-cleanup of expired sessions                                    │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                │                                              │
│                                ▼                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │                   Connection Pool Manager                               │ │
│  │  • One pool per database                                               │ │
│  │  • Min: 1, Max: 20 connections                                         │ │
│  │  • Thread-safe operations                                              │ │
│  │  • Auto-cleanup idle pools (30min)                                     │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                │                                              │
│                                ▼                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │                    SQL Agent (LangGraph)                                │ │
│  │  ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐        │ │
│  │  │ Generate │───▶│ Validate │───▶│ Execute  │───▶│ Success? │        │ │
│  │  │   SQL    │    │   SQL    │    │   SQL    │    │          │        │ │
│  │  └──────────┘    └──────────┘    └──────────┘    └────┬─────┘        │ │
│  │       ▲                                                 │               │ │
│  │       │                                                 │ Error         │ │
│  │       │                                                 ▼               │ │
│  │       │                                          ┌──────────┐           │ │
│  │       └──────────────────────────────────────────│  Retry   │           │ │
│  │                 (with error analysis)            │  Logic   │           │ │
│  │                                                  └──────────┘           │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                │                                              │
│                                ▼                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │                        LLM Service                                      │ │
│  │  ┌──────────┐      ┌──────────┐      ┌──────────┐                     │ │
│  │  │  OpenAI  │      │   vLLM   │      │  Ollama  │                     │ │
│  │  │ Provider │      │ Provider │      │ Provider │                     │ │
│  │  └──────────┘      └──────────┘      └──────────┘                     │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────┬───────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          CONNECTION POOLS                                    │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐            │
│  │  Pool: DB-A     │  │  Pool: DB-B     │  │  Pool: DB-C     │            │
│  │  Min: 1         │  │  Min: 1         │  │  Min: 1         │            │
│  │  Max: 20        │  │  Max: 20        │  │  Max: 20        │            │
│  │  Active: 5      │  │  Active: 3      │  │  Active: 12     │            │
│  └────────┬────────┘  └────────┬────────┘  └────────┬────────┘            │
│           │                    │                     │                       │
│           ▼                    ▼                     ▼                       │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐            │
│  │  PostgreSQL A   │  │  PostgreSQL B   │  │  PostgreSQL C   │            │
│  │  (Customer 1)   │  │  (Customer 2)   │  │  (Customer 3)   │            │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘            │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## Request Flow: New Connection

```
1. User Input (Frontend)
   └─▶ { host, port, database, username, password }

2. API: /database/connect
   └─▶ SessionManager.create_session()
       ├─▶ Generate UUID (session_id)
       ├─▶ Store connection info
       └─▶ Return session_id

3. ConnectionPoolManager
   └─▶ get_or_create_pool()
       ├─▶ Check if pool exists for this DB
       ├─▶ If not, create new ThreadedConnectionPool
       │   ├─▶ Min: 1 connection
       │   └─▶ Max: 20 connections
       └─▶ Return pool reference

4. Test Connection
   └─▶ pool.getconn() → Get connection from pool
       ├─▶ Execute: SELECT version(), table_count
       └─▶ pool.putconn() → Return to pool (NOT closed!)

5. Response to Frontend
   └─▶ { success: true, session_id: "abc-123", database_info: {...} }

6. Frontend Stores session_id
   └─▶ localStorage.setItem('session_id', 'abc-123')
```

---

## Request Flow: Query Execution

```
1. User Query (Frontend)
   └─▶ { question: "Show all users", session_id: "abc-123" }

2. API: /query
   └─▶ SessionManager.get_session(session_id)
       ├─▶ Validate session (not expired?)
       ├─▶ Update last_accessed
       └─▶ Get cached schema (if available)

3. Connection Pool
   └─▶ pool.getconn() → Reuse existing connection (FAST!)

4. SQL Agent Workflow
   ├─▶ Generate SQL using LLM
   │   └─▶ Uses cached schema from session
   ├─▶ Validate SQL
   ├─▶ Execute SQL using pooled connection
   ├─▶ If error → Analyze → Retry with hints
   └─▶ Return results

5. Return Connection
   └─▶ pool.putconn() → Back to pool (reusable!)

6. Response to Frontend
   └─▶ { results: [...], sql_query: "SELECT...", row_count: 42 }
```

---

## Multi-User Scenario

```
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│   User A    │       │   User B    │       │   User C    │
│   (DB-X)    │       │   (DB-X)    │       │   (DB-Y)    │
└──────┬──────┘       └──────┬──────┘       └──────┬──────┘
       │                     │                     │
       │  session_a          │  session_b          │  session_c
       │                     │                     │
       ▼                     ▼                     ▼
┌─────────────────────────────────────────────────────────────┐
│                   Session Manager                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  Session A   │  │  Session B   │  │  Session C   │      │
│  │  DB-X        │  │  DB-X        │  │  DB-Y        │      │
│  │  cached      │  │  cached      │  │  cached      │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└───────────┬─────────────────┬─────────────────┬─────────────┘
            │                 │                 │
            ▼                 ▼                 ▼
┌─────────────────────────────────────────────────────────────┐
│             Connection Pool Manager                          │
│  ┌────────────────────┐         ┌────────────────────┐      │
│  │    Pool: DB-X      │         │    Pool: DB-Y      │      │
│  │  ┌────┐  ┌────┐   │         │  ┌────┐  ┌────┐   │      │
│  │  │conn│  │conn│   │◀─────┬──│  │conn│  │conn│   │      │
│  │  │ 1  │  │ 2  │   │      │  │  │ 1  │  │ 2  │   │      │
│  │  └────┘  └────┘   │      │  │  └────┘  └────┘   │      │
│  │   ▲       ▲        │      │  │    ▲               │      │
│  └───┼───────┼────────┘      │  └────┼───────────────┘      │
│      │       │               │       │                       │
└──────┼───────┼───────────────┼───────┼───────────────────────┘
       │       │               │       │
     User A  User B          User C  User C
     (reuse) (reuse)         (new)   (reuse)

Result:
• Users A & B share Pool for DB-X (efficient!)
• User C gets separate Pool for DB-Y (isolated!)
• All connections are reused (fast!)
• Each user has own session (secure!)
```

---

## Memory & Performance Comparison

### Without Connection Pooling

```
Request 1:
  Create Connection (500ms) → Query (50ms) → Close (50ms) = 600ms
  Memory: 10MB per connection

Request 2:
  Create Connection (500ms) → Query (50ms) → Close (50ms) = 600ms
  Memory: 10MB per connection

Request 3:
  Create Connection (500ms) → Query (50ms) → Close (50ms) = 600ms
  Memory: 10MB per connection

Total Time: 1800ms
Total Memory: 30MB
```

### With Connection Pooling

```
Request 1:
  Create Pool (500ms) → Get Conn (5ms) → Query (50ms) → Return (5ms) = 560ms
  Memory: 10MB for pool

Request 2:
  Get Conn (5ms) → Query (50ms) → Return (5ms) = 60ms ← 10x faster!
  Memory: 0MB (reuse)

Request 3:
  Get Conn (5ms) → Query (50ms) → Return (5ms) = 60ms ← 10x faster!
  Memory: 0MB (reuse)

Total Time: 680ms ← 2.6x faster!
Total Memory: 10MB ← 3x less!
```

---

## Cleanup & Lifecycle

```
Session Lifecycle:
─────────────────
Create → Active → Idle → Expired → Deleted
  │       │        │       │          │
  0min   <60min   >60min  Cleanup   Memory freed

Connection Pool Lifecycle:
──────────────────────────
Create → Active → Idle → Expired → Closed
  │       │        │       │          │
  0min   <30min   >30min  Cleanup   Connections closed

Cleanup Thread (runs every 5 minutes):
──────────────────────────────────────
while True:
    sleep(300)
    ├─▶ Check all sessions → Remove expired
    └─▶ Check all pools → Close idle pools
```

---

## Error Recovery Flow

```
Query: "Show users from web_user table"
                │
                ▼
┌──────────────────────────────┐
│  Attempt 1: Generate SQL      │
│  Result: SELECT * FROM users  │  ← Wrong table name!
└───────────────┬────────────────┘
                │
                ▼
┌──────────────────────────────┐
│  Execute SQL                  │
│  Error: table "users" DNE     │
└───────────────┬────────────────┘
                │
                ▼
┌──────────────────────────────────────────────────────┐
│  Error Analysis                                       │
│  ❌ Table 'users' DOES NOT EXIST!                    │
│  ✓ Available tables: web_user, role_permissions...  │
│  💡 Did you mean: web_user?                          │
│                                                       │
│  Focused Schema:                                      │
│  Table: web_user                                      │
│  Columns:                                             │
│    - id (integer) NOT NULL                            │
│    - username (varchar) NOT NULL                      │
│    ...                                                │
└────────────────────┬──────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────┐
│  Attempt 2: Generate SQL (with hints) │
│  Result: SELECT * FROM web_user       │  ← Correct!
└────────────────────┬──────────────────┘
                     │
                     ▼
┌──────────────────────────────┐
│  Execute SQL                  │
│  Success! Returns 42 rows     │
└───────────────────────────────┘
```

---

This architecture enables DatabaseAI to handle enterprise-scale workloads efficiently! 🚀
